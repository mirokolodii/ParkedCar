<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".views.bluetooth.BluetoothFragment">

   <include android:visibility="gone"
       android:id="@+id/bluetooth_content"
       layout="@layout/fragment_bluetooth_content" />

    <include android:visibility="visible"
        android:id="@+id/disabled_bluetooth_content"
        layout="@layout/fragment_bluetooth_disabled_content" />

</FrameLayout>